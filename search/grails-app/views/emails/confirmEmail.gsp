dear ${toaddr},

<p>This is the data controller at <a href="${config.baseURL}">The open local information directory</a>. If you are confirming this email address as belonging to
this account, please click <a href="${config.baseURL}/confirm?id=${id}&email=${tracker.guid}"> here to confirm. If you did not request confirmation, this
email can be safely ignored.
</p>
